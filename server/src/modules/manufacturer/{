'const getDashboardStats = async (userId) = try { const manufacturer = await prisma.manufacturer.findUnique({ where: { userId: parseInt(userId) }, include: { manufacturerProducts: { include: { product: true, }, }, companyInfo: true, orders: true, }, }); if (!manufacturer) { throw new Error("Manufacturer not found"); } const totalProducts = manufacturer.manufacturerProducts?.length 
