const { Router } = require('express');

const router = Router();

// Placeholder routes for admin management
router.get('/', (req, res) => {
  res.json({ message: 'Admin routes not implemented yet' });
});

module.exports = router;
